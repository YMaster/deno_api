/**
 * unstable 的功能
 * 需要 --unstable 参数
 * signo： 签名
 */

import * as singal from 'https://deno.land/std/signal/mod.ts'

// TODO: 等待 stable

// function signal(signos_0: number, ...signos_1: number[]): AsyncIterable<void> & singal.Disposable
// console.log(singal.signal(1, 2, 3, 4, 5))

// function onSignal(signo: number, callback: () => void): singal.Disposable
// singal.onSignal(12, () => {

// })