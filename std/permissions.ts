/**
 * unstable 的功能
 * 需要 --unstable 参数
 */

import * as permissions from 'https://deno.land/std/permissions/mod.ts'

// permissions.grantOrThrow().then((res) => {
//   console.log(res)
// })

// Deno.permissions.request